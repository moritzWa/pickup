overwrite: true
schema: "../schema.graphql"
config:
    scalars:
        Date: Date
generates:
    # src/core/surfaces/graphql/generated/types.ts:
    #     plugins:
    #         - "typescript"
    #         - "typescript-resolvers"
    ../app/src/api/graphql/generated/types.ts:
        plugins:
            - "typescript"
            - "typescript-resolvers"
